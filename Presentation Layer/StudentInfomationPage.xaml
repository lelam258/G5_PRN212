<Page x:Class="Presentation_Layer.StudentInformationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="StudentInformationPage"
      Background="{DynamicResource WindowBackgroundColor}">

    <Border Padding="30"
            Background="{DynamicResource WindowBackgroundColor}">

        <Grid>
            <StackPanel Orientation="Vertical" VerticalAlignment="Top" Margin="0,0,0,20">
                <!-- Tiêu đề -->
                <TextBlock Text="👤 Thông tin cá nhân"
                           FontSize="26"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource TextForegroundColor}"
                           Margin="0,0,0,20" />

                <!-- Biểu mẫu -->
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Email -->
                    <TextBlock Grid.Row="0" Text="Email:" FontWeight="Bold" 
                               Foreground="{DynamicResource TextForegroundColor}" Margin="0,0,10,10"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Email}" Margin="0,0,0,10" />

                    <!-- SĐT -->
                    <TextBlock Grid.Row="1" Text="Số điện thoại:" FontWeight="Bold"
                               Foreground="{DynamicResource TextForegroundColor}" Margin="0,0,10,10"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding PhoneNumber}" Margin="0,0,0,10" />

                    <!-- Ngày sinh -->
                    <TextBlock Grid.Row="2" Text="Ngày sinh:" FontWeight="Bold"
                               Foreground="{DynamicResource TextForegroundColor}" Margin="0,0,10,10"/>
                    <DatePicker Grid.Row="2" Grid.Column="1" SelectedDate="{Binding DateOfBirth}" Margin="0,0,0,10" />

                    <!-- Avatar -->
                    <TextBlock Grid.Row="3" Text="Ảnh đại diện:" FontWeight="Bold"
                               Foreground="{DynamicResource TextForegroundColor}" Margin="0,0,10,10"/>
                    <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal">
                        <Image Width="60" Height="60" Source="{Binding AvatarPath}" Margin="0,0,10,0"/>
                        <Button Content="Chọn ảnh..." Click="UploadAvatar_Click" />
                    </StackPanel>
                </Grid>

                <!-- Nút Lưu -->
                <Button Content="Lưu thay đổi"
                        Width="150"
                        Height="40"
                        Background="#3498DB"
                        Foreground="White"
                        FontWeight="Bold"
                        Click="SaveChanges_Click"
                        HorizontalAlignment="Left" />
            </StackPanel>
        </Grid>
    </Border>
</Page>
